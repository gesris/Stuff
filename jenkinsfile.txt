pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
		        sh 'ls -a'
		        sh 'python3 python_test.py'

            }
        }
    }
}
